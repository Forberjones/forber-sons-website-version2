import ServicesExpanded from "../components/servicesExpanded";

export const metadata = {
  title: "Services | Forber & Sons LTD",
  description:
    "EICR, fixed wiring inspection & testing, fire alarms & emergency lighting, PAT testing, maintenance and domestic installs.",
};

export default function ServicesIndexPage() {
  return <ServicesExpanded />;
}
